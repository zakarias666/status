owner: zakarias666
repo: status

sites:
  - name: Website
    url: https://zbj.dk

# Response slower than this (ms) marks a site "degraded" instead of "up".
# assignees:
# status-website is intentionally omitted — the status page is the custom
# widget site in docs/, not Upptime's built-in Svelte site.
